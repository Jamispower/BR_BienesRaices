header(class="bg-indigo-500 p-4")  
    .container.mx-auto.flex.justify-between.items-center
        a(href="/")
            h1(class="text-2xl text-white font-extrabold text-center") Bienes    
                span.font-normal Raices
        
        nav(class="my-5 tex-sm md:flex md:items-center font-bold md:gap-3 text-white")
            a(href="/mis-propiedades") Mis propiedades
            a(href="") Mi Perfil
            form(action="/auth/cerrar-sesion" method="POST")
                input(type="hidden" name="_csrf" value=csrfToken)
                button(type="submit" class="bg-indigo-700 py-2 px-10 rounded-lg ") Cerrar Sesion