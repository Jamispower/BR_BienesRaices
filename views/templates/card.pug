div(class="bg-white shadow rounded-lg p-4")
    img(src=`/uploads/${departamento.img}`, alt=departamento.titulo class="w-full h-72 object-cover rounded-lg")
    div(class="p-4 space-y-2")
        h3(class="text-lg font-bold text-gray-800")= departamento.titulo
        div(class="grid grid-cols-2 justify-start gap-2")
            p(class="text-gray-600 text-sm") Habitaciones:  
                span(class="text-gray-800 font-bold")= departamento.habitaciones
            p(class="text-gray-600 text-sm") Baños:  
                span(class="text-gray-800 font-bold")= departamento.wc
        p(class="text-gray-600 text-sm") Precio:  
            span(class="text-gray-800 font-bold")= departamento.precio.precio
        a(href=`/propiedad/${departamento.id}` class="block text-center bg-indigo-500 hover:bg-indigo-700 text-white font-bold uppercase py-1 px-4 rounded-lg") Ver Propiedad